from followup_generator import generate_followup

result = generate_followup(
    "Should AI replace teachers?",
    "AI should assist teachers rather than replace them because teachers provide emotional support."
)

print(result)